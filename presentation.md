# 这个项目是什么

一个**面向硬件工程师**的 AI Agent 驱动 PCB 元器件库。 用户用**自然语言**输入（"*0805 封装 10kΩ 电阻库存多少*"）， 系统**自动翻译为 SQL** 并返回结果。支持**查询 + CRUD + 选型建议**。

## 前端

（frontend/，原生 HTML/CSS/JS，端口 3001）：

- 渲染 UI：搜索框、筛选栏、物料表格、弹窗表单
- 收集用户输入（自然语言 query / 表单字段），调用后端 API
- 展示结果（数据表格 + AI 建议文字）
- 写操作的二次确认弹窗

## 后端

（backend/，Express + SQLite，端口 3000）：

- 提供 REST API（/api/query、/api/components、/api/categories 等）
- 三层 NL→SQL 引擎：规则引擎 → 缓存 → LLM，把自然语言转成 SQL
- 执行 SQL 操作 SQLite 数据库
- SQL 安全防护（DDL 拦截、多语句检测、强制 WHERE）
- 写操作的 LLM 生成 → 守卫校验 → 前端确认 → 真正执行

---------------------------------------------



# Claude Code & Skills — AI 辅助开发工作流

**这个项目由 Claude Code 辅助开发。**从需求分析、架构设计、代码编写到代码审查，全程使用 CC 的 Agent、Skill、Plan Mode 功能。以下是实际使用的工具和工作流。

## 人为提出要求

 整体需求：

- PCB 元器件库管理、NL→SQL、增删改查、AI 选型


- 前后端分离架构 —— 这是 Web 项目的行业标准模式，人力主决定
- 具体的数据库表结构（6 张表的字段定义在 Request.txt 中）

## AI用于细节化＆ 执行

### EnterPlanMode — 计划驱动开发

所有非平凡改动（三层引擎、AI 模式重构、UI 专业化）都先进入 Plan Mode：

1. **探索代码库** → 理解现有架构
2. **设计方案** → 修改文件清单 + 数据流图 + 验证方案
3. **用户审批** → Plan 获批准后才开始写代码
4. **按 Plan 执行** → 不偏离，不"顺手"改其他东西



### Brainstorming — 从模糊想法到清晰设计

项目用 brainstorming 解决了"查询策略选什么"这个核心架构决策：三轮提问（查询策略 → UI 模式 → 写安全），最终确定**规则优先 + 混合 UI + 二次确认**方案。

--------------------------------------------



# CLAUD.md

1. **编码前思考**：`<thinking>` 标签暴露思路 · 遇歧义即阻断 · 评估影响半径
2. **简洁优先**：标准库优先 · 奥卡姆剃刀 · 不过度防御编程
3. **精准修改**：严禁 `...` 占位 · 风格变色龙 · 只清孤儿代码
4. **契约闭环**：TDD 步骤 · 处理隐性契约（空值/并发/超时）
5. **提交纪律**：**提交和推送必须经过用户同意**

----------------------------------------



#  Git 工作流

## Git 是 AI Coding 的天然搭档

AI 辅助编程有一个核心矛盾：**AI 写得快，但可能写错**。Git 恰好解决了这个问题——它提供了一张随时可用的"撤销"网。两者互补，缺一不可。

#### AI 编程的天然风险

- AI 一次生成大量代码，人工逐行审查跟不上
- AI 可能"顺手"改不该改的文件（风格、import、空行）
- 多轮对话中，AI 可能遗忘之前的约定
- 一次错误的修改可能连锁影响多个模块

#### Git 提供的保护

- `git diff` — AI 的每一次修改都无处遁形，逐行审视
- `git reset --hard` — AI 改坏了？一秒回到修改前
- `git log` — 追溯每次 AI 协作的来龙去脉
- `git bisect` — 定位是哪个 AI 改动引入了 bug

## 实际工作流：人 + AI + Git协作

| 环节     | 谁做                   | Git 的角色                                   |
| -------- | ---------------------- | -------------------------------------------- |
| 修改代码 | AI 主导                | 工作区变动，尚未追踪                         |
| 审查变更 | **人** 通过 `git diff` | Git 展示每一处改动，无处隐藏                 |
| 提交     | AI 执行，**人审批**    | commit message 标注 `Co-Authored-By: Claude` |
| 推送     | AI 执行，**人审批**    | 代码进入远程仓库，可跨机器迁移               |
| 回滚     | **人** 决定            | `git reset` — AI 改错了也能一秒撤销          |

--------------------------------





# 三层 NL2SQL 架构

用户说人话，它输出 SQL，执行后返回结果。

## 引擎的三条路径

![image-20260608115831665](C:\Users\chengbigdan\AppData\Roaming\Typora\typora-user-images\image-20260608115831665.png)

三条路径，按优先级：

1. **规则引擎**（<1ms）—— 6 类正则匹配封装/分类/制造商/库存/价格/料号，覆盖 80% 查询
2. **缓存层**（<1ms）—— 问过的问题直接返回上次 SQL，30 分钟有效
3. **LLM**（1~3s）—— 规则和缓存都兜不住时，才调用大模型理解复杂语义



## 搜索模式：Fast vs AI

**80% 的查询是"封装 + 分类"模式**（"0805 电阻"、"SOT-23 MOS管"），正则毫秒级覆盖。只有复杂语义才走到 LLM。

#### 快速模式 (Fast)

规则 → 缓存，全毫秒级。未命中直接返回空 + 建议切换 AI。
**零 AI 调用、零成本**

#### 快速模式 (Fast)

规则 → 缓存，全毫秒级。未命中直接返回空 + 建议切换 AI。
**零 AI 调用、零成本**











